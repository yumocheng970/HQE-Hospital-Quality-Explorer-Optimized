##
Group members: Yumo Cheng, Dawei Feng, Iris Ge

##
## Project Structure

```
hospital-quality-explorer/
│
├── docs/                         # Wireframes and design sketches
│
├── src/
│   ├── etl/
│   │   └── load.py               # Download CSVs → clean → load into SQLite
│   │
│   ├── server/
│   │   ├── main.py               # Flask entry point, registers blueprints
│   │   ├── db.py                 # SQLite connections: hospital_quality.db (read-only), app.db (read-write)
│   │   └── routers/
│   │       └── hospitals.py      # GET /api/hospitals
│   │                             # GET /api/hospitals/:id
│   │                             # GET /api/lookup/states
│   │
│   ├── client/
│   │   ├── index.html
│   │   ├── vite.config.js        # Proxies /api → localhost:3001
│   │   ├── tailwind.config.js
│   │   ├── main.jsx              # React entry point
│   │   ├── App.jsx               # Route configuration
│   │   ├── api/
│   │   │   └── index.js          # Fetch wrapper
│   │   ├── pages/
│   │   │   ├── SearchPage.jsx
│   │   │   └── HospitalDetailPage.jsx
│   │   └── components/
│   │       ├── Navbar.jsx
│   │       ├── SearchBar.jsx
│   │       ├── FilterPanel.jsx
│   │       └── HospitalCard.jsx
│   │
│   ├── hospital_quality.db       # Generated by ETL (gitignored)
│   ├── app.db                    # Users and sessions (gitignored)
│   └── .env                      # Local dev only (gitignored)
│
├── DECISIONS.md                  # Design decision log
├── README.md
├── requirements.txt              # Python dependencies
├── package.json                  # JavaScript dependencies
└── .gitignore
```
